name: Deploy to EasyPanel

on:
  push:
    branches:
      - master
permissions: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger EasyPanel deploy
        run: curl -fsS --max-time 30 --retry 3 -X POST "${{ secrets.EASYPANEL_DEPLOY_URL }}"
